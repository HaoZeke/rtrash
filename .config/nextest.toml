# cargo-nextest profiles (see https://nexte.st/)
[profile.default]
slow-timeout = { period = "60s", terminate-after = 2 }

[profile.ci]
retries = 1
fail-fast = false
slow-timeout = { period = "120s", terminate-after = 2 }
